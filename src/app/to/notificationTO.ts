export class notificationTO {

  private heading;
  private message;
  private headingColor;
  private headingTextColor;
  private bodyColor;
  private bodyTextColor;
  private timeNotification;
  private timerInterval;

  constructor() {
  }
}
